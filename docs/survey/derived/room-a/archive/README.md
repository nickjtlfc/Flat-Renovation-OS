# Room A reconstruction archive

This folder preserves the original Room A reconstruction pilot and its report/drawing. It is provenance for the promoted baseline in the parent directory, not active geometry.

Historical generators are under `scripts/archive/2d-reconstruction/`.
