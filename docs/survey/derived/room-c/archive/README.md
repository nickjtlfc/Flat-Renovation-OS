# Room C reconstruction history

This directory contains the immutable development artefacts behind the promoted `ROOM_C_FINAL_2D_WORKING_BASELINE_v1_0`, including the former accepted provisional v1.0 baseline and its acceptance note:

- v0.1 — first-pass A1 shell and finite-thickness PO/PI partition;
- v0.2 — object-layer integration;
- v0.3 — corrected RC-19 endpoint and D2 closing plane plus local height evidence;
- v0.4 — corrected CP1 and W2 vertical interpretations.

These files are provenance, not competing active Room C baselines. Their filenames and contents were preserved when they were moved from the parent directory.

The historical scripts are in `scripts/archive/2d-reconstruction/`. Historical files retain their original logical provenance descriptions where changing them would falsify the recorded history.

Do not resume Room C-only optimisation from these files. The active baseline is `ROOM_C_FINAL_2D_WORKING_BASELINE_v1_0` in the parent directory; reopen local geometry only for an explicitly scoped evidence-led task.
